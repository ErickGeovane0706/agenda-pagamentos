spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/agenda_pagamentos}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASS:postgres}
  jpa:
    show-sql: true

jwt:
  secret: DuX/YP1e/lLly8HzTYR4AQtnhyDXlhppYnSplw8ftLA=
  expiration: 86400000

# Dev roda em http://localhost — cookie Secure quebraria o login local.
security:
  cookie-secure: false

r2:
  account-id: seu_account_id
  access-key: sua_access_key
  secret-key: sua_secret_key
  bucket: agenda-pagamentos-arquivos

logging:
  level:
    com.agenda: DEBUG
    org.hibernate.SQL: DEBUG

whatsapp:
  webhook-verify-token: token-local-dev-trocar

anthropic:
  api-key: sua-chave-anthropic-aqui
  model: claude-haiku-4-5

openai:
  api-key: sua-chave-openai-aqui
