package com.agenda.domain.coluna;

/**
 * Enum que vincula colunas extras ao tipo de pagamento.
 * Uma coluna extra de BOLETO só aparece na tela de boletos, etc.
 */
public enum TipoPagamento {
    BOLETO, PIX, CHEQUE
}